# ── Node ────────────────────────────────
node_modules/
dist/
.vite/
playwright-report/
test-results/

# ── Dataset gốc — chỉ dùng để train trên máy local
# KHÔNG commit lên git (hàng trăm MB ảnh)
dataset/
dataset_review/
archive.zip
model_candidates/

# ── Model files ─────────────────────────
# Model gốc .h5 - quá nặng, không commit
# Nếu cần deploy full-stack, upload riêng lên server
trash_classifier_model.h5
*.h5
saved_model_temp/
saved_model_tmp/

# Model TF.js dùng trực tiếp trên frontend; cần commit cả model.json và các shard .bin.

# Ảnh crop chỉ dùng để debug nhận diện trên máy local.
debug_camera_crop*.jpg
debug_paper_small_crop.jpg

# ── Python cache ────────────────────────
__pycache__/
*.pyc
*.pyo
.env
*.log

# ── OS ──────────────────────────────────
.DS_Store
Thumbs.db
*.swp

# ── IDE ─────────────────────────────────
.vscode/
.idea/
*.code-workspace
